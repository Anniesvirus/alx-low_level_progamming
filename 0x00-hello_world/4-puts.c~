#include<stdio.h>
/**
 * main - Entry point
 *
 * Description: 'This program would print an information'
 *
 * Return: Always (0) success
 *\
int main(void)
{
	char Anniesvirus[] = "\"programming is like building a multilingual puzzle";

	puts(Anniesvirus);

	return (0);

}
